org.gradle.jvmargs=-Xmx1G -XX:MaxMetaspaceSize=512M -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental path cache cannot relativize plugin sources across
# drives (pub cache on C:, project on D:) and fails with "this and base files
# have different roots". Disable incremental compilation to avoid it.
kotlin.incremental=false
